server:
  address: ":8080"

strategy:
  type: round_robin

targets:
  - id: api_1
    address: localhost:9001

  - id: api_2
    address: localhost:9002

health:
  enabled: true
  interval: 2s
  timeout: 1s

metrics:
  enabled: true
  address: ":9090"

logging:
  level: info
